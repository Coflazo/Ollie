"""Ollie, a local-first dating simulator.

Everything runs on the user's machine: the model via Ollama, the memory in SQLite, the
corpus index on disk. Nothing leaves the device.
"""

__version__ = "0.1.0"
